#!/bin/sh

mic=""
vol=$(sndioctl -n output.level | sed 's/0\.//' | sed 's/.$/%/' | sed 's/\.//')

if [ $(sndioctl -n input.mute) = "1" ]; then
  mic=" "
fi

if [ $(sndioctl -n output.mute) = "1" ]; then
  vol="󰝟"
fi

echo "$vol$mic"

